/* eslint-env browser */
const prArea = document.getElementById('projects');
const addPro = document.querySelector('.btn');
const formPr = document.getElementById('prForm');
const todoH = document.querySelector('.todoH');
const showTodoForm = document.querySelector('.to-fa');
const todoForm = document.querySelector('.todoForm');
const saveTodo = document.getElementById('saveTodo');
const closeTodoForm = document.getElementById('closeTodo');
const addF = document.querySelector('.addF');
const updtButton = document.getElementById('update');
const proFa = document.querySelector('.pro-fa');
const prSub = document.getElementById('prSub');
const prInp = document.querySelector('.prInput');

export {
  prArea,
  addPro,
  formPr,
  todoH,
  showTodoForm,
  todoForm,
  saveTodo,
  closeTodoForm,
  addF,
  updtButton,
  proFa,
  prSub,
  prInp
};
